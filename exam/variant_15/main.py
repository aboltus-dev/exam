# Вариант 15
# По заданию нужно проверить, что функция возвращает True для строки длиной ровно 4 символа.
# Этот файл содержит саму функцию.
# main.py можно было бы назвать и иначе: string_check.py, app.py и т.д.

def is_length_4(s):
    return len(s) == 4